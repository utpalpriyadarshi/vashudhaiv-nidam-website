# Office lock/temp files
~$*
*.tmp

# OS cruft
.DS_Store
Thumbs.db

# Source/reference material (scans, source docs) — not part of the
# deployed site; some contain sensitive info (bank details, IDs) that
# shouldn't sit in a public repo's history.
/reference-material/
